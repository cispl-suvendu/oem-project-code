import React from "react";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Exams",
  description: "Add Edit Exams",
};
export default function Layout({ children }) {
  return <>{children}</>;
}
